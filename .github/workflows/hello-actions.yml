name: Hello Actions

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  hello:
    runs-on: ubuntu-latest

    steps:
      - name: Show repository and runner
        run: |
          echo "Repository: ${{ github.repository }}"
          echo "Runner OS: ${{ runner.os }}"